@model SchoolApp.Models.UserGroupInstance

@{
    ViewBag.Title = "Edit";
}


@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

        @Html.HiddenFor(model => model.UserGroupInstanceID)

        <div class="editor-label">
            @Html.LabelFor(model => model.AttendanceTaken)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.AttendanceTaken)
            @Html.ValidationMessageFor(model => model.AttendanceTaken)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.UserId, "User")
        </div>
        <div class="editor-field">
            @Html.DropDownList("UserId", String.Empty)
            @Html.ValidationMessageFor(model => model.UserId)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.GroupInstanceId, "GroupInstance")
        </div>
        <div class="editor-field">
            @Html.DropDownList("GroupInstanceId", String.Empty)
            @Html.ValidationMessageFor(model => model.GroupInstanceId)
        </div>

        <p>
            <button type="submit" class="btn btn-primary"><i class="icon-save"></i> Save</button>
            <a href="@Url.Action("Index")" class="btn">Back to list</a>
        </p>
}


@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
